# repos

CLI for easier management of git repositories in local computer

Features

- clone repositories to selected folder
- set folder as repositories store
- create branches and commit messages in configured format
- protection from commiting code with unwanted text like auth tokens etc (require configuration)